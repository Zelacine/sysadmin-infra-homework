<?php
$env = getenv('APP_ENV') ?: '{{ web_app_env }}';
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>PHP App</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            color: #333;
            
            padding: 20px;
        }
        h1 {
            color: #0073e6;
        }
        strong {
             color: #e60000;
             font-size: 1.2em;
          }
        .content {
            max-width: 600px;
            margin: auto;
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);            
     </style>
</head>
<body>
     <div class="content">
    <h1>Welcome to the PHP application</h1>
    <p>Environment: <strong><?php echo htmlspecialchars($env, ENT_QUOTES, 'UTF-8'); ?></strong></p>
    <ul>
    <li>Health check <a href="/healthz" target="_blank">healthz</a></li>
    <li>Health check  php-fpm <a href="/healthz-php" target="_blank">health php-fpm</a></li>
    </ul>
    </div>
</body>
</html>

