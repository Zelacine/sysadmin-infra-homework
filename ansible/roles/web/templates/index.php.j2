<?php
$env = getenv('APP_ENV') ?: '{{ web_app_env }}';
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>PHP App</title>
</head>
<body>
    <h1>Welcome to the PHP application</h1>
    <p>Environment: <strong><?php echo htmlspecialchars($env, ENT_QUOTES, 'UTF-8'); ?></strong></p>
    <p> Project: <strong>{{ web_project_name }}</strong></p>
    <b>I know it's dangerous to use phpinfo() in production, but this is just a homework assignment.</b>
    <p><?php phpinfo(); ?></p>
</body>
</html>

